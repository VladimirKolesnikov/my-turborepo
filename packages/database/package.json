{
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "files": [
    "dist",
    "drizzle",
    "drizzle-mastra"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "default": "./dist/schema.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx src/db-seed/seed.ts",
    "db:generate:mastra": "drizzle-kit generate --config drizzle.mastra.config.ts",
    "db:migrate:mastra": "drizzle-kit migrate --config drizzle.mastra.config.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "dotenv": "^16.4.7",
    "dotenv-expand": "^13.0.0",
    "drizzle-kit": "^0.30.4",
    "tsx": "^4.19.3",
    "typescript": "5.9.2"
  }
}
